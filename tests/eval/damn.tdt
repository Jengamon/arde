async
edge(x, y, 1).
edge(y, z, 1).
edge(z, w, 1).
add(1, 1, 2).
add(1, 2, 3).
add(2, 1, 3).

path(X, Y, L) :- edge(X, Y, L).
path(X, Z, TL) :- path(X, Y, A), edge(Y, Z, B), add(A, B, TL).

path(x, w, 3)?
----
----
